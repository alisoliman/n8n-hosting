apiVersion: v1
kind: Service
metadata:
  name: hello-world-mcp-service
spec:
  type: ClusterIP  # Use ClusterIP for internal access
  selector:
    app: hello-world-mcp
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
